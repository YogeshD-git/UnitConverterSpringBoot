package com.UnitConverter.UnitConverter.Enumeration;

//In Java, an enum (short for enumeration) is a special data type that represents a fixed set of constants. It's used to define a collection of predefined, 
//unchangeable values that represent a type's possible states.

public enum Quantities {
	 LENGTH,
	    VOLUME,
	    WEIGHT,
	    TEMPERATURE
}
